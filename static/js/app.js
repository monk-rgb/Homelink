document.querySelectorAll('[data-step]').forEach(btn=>btn.addEventListener('click',()=>{const key=btn.dataset.step;const dir=Number(btn.dataset.dir);const input=document.getElementById(key);if(!input)return;let v=Math.max(0,Math.min(20,Number(input.value)+dir));input.value=v;document.getElementById(key+'Out').textContent=v;}));const form=document.getElementById('predictForm');if(form){const locationFields=document.querySelectorAll('[name="location_method"]');const addressFields=document.getElementById('addressFields'),yardcodeField=document.getElementById('yardcodeField');const syncLocation=()=>{const yard= document.querySelector('[name="location_method"]:checked')?.value==='yardcode';addressFields.hidden=yard;yardcodeField.hidden=!yard;document.getElementById('state').disabled=yard;document.getElementById('city').disabled=yard};locationFields.forEach(x=>x.addEventListener('change',syncLocation));syncLocation();form.addEventListener('submit',e=>{const yard= document.querySelector('[name="location_method"]:checked')?.value==='yardcode';const required=yard?['yardcode','property_type','area','age']:['state','city','property_type','area','age'];let missing=required.some(k=>!form.querySelector(`[name="${k}"]`)?.value);const b=form.querySelector('[name="bedrooms"]')?.value;const ba=form.querySelector('[name="bathrooms"]')?.value;const p=form.querySelector('[name="parking"]')?.value;if(!b||!ba||!p||Number(b)<1||Number(ba)<1||missing){e.preventDefault();document.getElementById('resultCard').innerHTML='<div class="error-estimate">Sorry you have to fill the required information for accurate result</div>';document.getElementById('resultCard').scrollIntoView({behavior:'smooth',block:'center'});}})}

const navMenuToggle=document.getElementById('navMenuToggle');const mobileNav=document.getElementById('mobileNav');
if(navMenuToggle&&mobileNav){const setMenu=open=>{mobileNav.hidden=!open;navMenuToggle.setAttribute('aria-expanded',String(open));navMenuToggle.setAttribute('aria-label',open?'Close navigation menu':'Open navigation menu')};setMenu(false);navMenuToggle.addEventListener('click',()=>setMenu(mobileNav.hidden));mobileNav.addEventListener('click',e=>{if(e.target.tagName==='A')setMenu(false)});window.addEventListener('resize',()=>{if(window.innerWidth>620&&!mobileNav.hidden)setMenu(false)});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!mobileNav.hidden)setMenu(false)})}

const themeToggle=document.getElementById('themeToggle');
if(themeToggle){const root=document.documentElement,label=document.getElementById('themeLabel');const syncTheme=()=>{const light=root.dataset.theme==='light';label.textContent=light?'Dark mode':'Light mode';themeToggle.setAttribute('aria-label',light?'Switch to dark mode':'Switch to light mode');themeToggle.setAttribute('aria-pressed',String(light))};syncTheme();themeToggle.addEventListener('click',()=>{root.dataset.theme=root.dataset.theme==='light'?'dark':'light';localStorage.setItem('estimate-theme',root.dataset.theme);syncTheme()})}
